# Sistema RFID para nadadores (R300 YRM200 RFID reader module)
# Python 3.6+ requerido.
# Dependencias de ejecución: solo biblioteca estándar (socket, etc.).
# Tests (opcional): pytest
#   pip install pytest
